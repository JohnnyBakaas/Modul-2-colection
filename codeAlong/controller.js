// const deleteMovie = (movieIndex) => {
//   model.movies.splice(movieIndex, 1);
//   view();
// };

// const renameMovie = (movieIndex, newMovie) => {
//   model.movies[movieIndex].title = newMovie; //model.input.changetitel;
//   view();
// };

// renameMovie(0, "Alt for masse skifting av gir");
